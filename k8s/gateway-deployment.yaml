apiVersion: apps/v1
kind: Deployment
metadata:
  name: convoyia-gateway
  namespace: convoyia
  labels:
    app: convoyia-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: convoyia-gateway
  template:
    metadata:
      labels:
        app: convoyia-gateway
    spec:
      containers:
        - name: convoyia-gateway
          image: goweyy/convoyia-gateway:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: convoyia-config
            - secretRef:
                name: convoyia-secrets
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: convoyia-gateway
  namespace: convoyia
spec:
  type: LoadBalancer
  selector:
    app: convoyia-gateway
  ports:
    - port: 80
      targetPort: 8080
