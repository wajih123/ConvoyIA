apiVersion: apps/v1
kind: Deployment
metadata:
  name: convoyia-verifier
  namespace: convoyia
  labels:
    app: convoyia-verifier
spec:
  replicas: 2
  selector:
    matchLabels:
      app: convoyia-verifier
  template:
    metadata:
      labels:
        app: convoyia-verifier
    spec:
      containers:
        - name: convoyia-verifier
          image: goweyy/convoyia-verifier:latest
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: convoyia-config
            - secretRef:
                name: convoyia-secrets
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: convoyia-verifier
  namespace: convoyia
spec:
  selector:
    app: convoyia-verifier
  ports:
    - port: 80
      targetPort: 8082
