apiVersion: apps/v1
kind: Deployment
metadata:
  name: convoyia-tracker
  namespace: convoyia
  labels:
    app: convoyia-tracker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: convoyia-tracker
  template:
    metadata:
      labels:
        app: convoyia-tracker
    spec:
      containers:
        - name: convoyia-tracker
          image: goweyy/convoyia-tracker:latest
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: convoyia-config
            - secretRef:
                name: convoyia-secrets
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: convoyia-tracker
  namespace: convoyia
spec:
  selector:
    app: convoyia-tracker
  ports:
    - port: 80
      targetPort: 8085
